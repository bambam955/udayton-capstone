# Copy this file to `assets/env/local.env` for local development when you need
# a real Mapbox token bundled into the app.
#
# CI should prefer `--dart-define=ACCESS_TOKEN=...` so secrets do not live in
# the repository checkout.
ACCESS_TOKEN=

<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Local emulator profiling can still call the host API over HTTP. -->
    <application android:usesCleartextTraffic="true"/>
</manifest>

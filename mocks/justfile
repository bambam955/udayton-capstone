set shell := ["bash", "-cu"]

# List available recipes
default:
    @just --list

# Run mock endpoint smoke tests
test:
    npm run test

# Validate mock JSON files and compose config
check:
    npm run check

# Normalize mock JSON formatting
format:
    npm run format

# Install dependencies
deps:
    # Use install over clean-install to ensure package-lock.json can be updated.
    # This recipe will only ever be run in a development context.
    npm i --no-fund

clean:
    @echo "Nothing to do."

build:
    @echo "Nothing to do."

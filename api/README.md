# BizRush API

Node/Express backend service with PostgreSQL access via `pg` + `Kysely`.

## Scripts

- `npm run dev`: start in watch mode
- `npm run build`: compile TypeScript
- `npm run check`: type-check only
- `npm test`: run unit tests

# Dev container — reuses the apps base image, mounts the full project.
services:
  devcontainer:
    build:
      context: ../apps
      target: base
    volumes:
      - ..:/workspace
      - pub-cache:/root/.pub-cache
    working_dir: /workspace
    tty: true

volumes:
  pub-cache:

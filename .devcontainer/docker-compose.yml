# Dev container override — mounts the full project instead of just app/
services:
  devcontainer:
    build:
      context: ../apps
      target: base
    volumes:
      - ..:/workspace
      - pub-cache:/root/.pub-cache
    working_dir: /workspace
    tty: true

volumes:
  pub-cache:
